# OpenHDS Pro 2.0.114

## Fixed

- `Resume required step` now opens Visit Registration after a missing draft is safely cleared.
- The restarted form retains the existing visit, compound, household and selected interviewee context.
- The recovery route does not create a second visit record.

## Safe update

Install this APK over the existing OpenHDS Pro installation. Do not uninstall the current app, because uninstalling can remove locally stored fieldwork.
