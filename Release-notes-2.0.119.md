# OpenHDS Pro 2.0.119

- Removed the redundant **Fieldworker:** prefix from top-right account labels.
- Kept the account icon, signed-in user name and fieldworker code unchanged.

Install this APK over the existing OpenHDS Pro app. Do not uninstall the app, because uninstalling removes locally stored application data.
