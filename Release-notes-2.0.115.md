# OpenHDS Pro 2.0.115

- Saves the encrypted OpenHDS server URL in a dedicated installation-private store.
- Migrates an existing saved URL automatically.
- Keeps the setting across app restarts and signed in-place updates.
- Removes the setting when Android uninstalls the app.
- Confirms the encrypted setting is durably stored before reporting success.
